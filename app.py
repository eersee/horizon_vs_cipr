import os
from flask import Flask, render_template, jsonify

app = Flask(__name__)

# ------------------------------------------------------------
# Запасное текстовое содержимое слайдов (если нет картинки)
# ------------------------------------------------------------
slides_data = {}

# Заглушки для всех слайдов от 1 до 106
for i in range(1, 107):
    slides_data[i] = {
        "content": f"<p>Содержание слайда {i} из презентации «Горизонт-ВС».</p>"
    }

# Индивидуальное содержимое (можно дополнить)
slides_data[90] = {
    "content": "<p>Ирина Барламова, Денис Обиднык<br>+7 981 751 4412<br>info@rt-intech.ru<br>www.rt-intech.ru</p>"
}
for i in range(91, 96):
    slides_data[i] = {"content": f"<p>О продукте — информация (слайд {i}).</p>"}
slides_data[96] = {"content": "<p>ФСТЭК — сертификация, соответствие требованиям.</p>"}
slides_data[97] = {"content": "<p>Безопасная разработка — DevSecOps, анализ уязвимостей.</p>"}
slides_data[98] = {"content": "<p>Экосистема — общие принципы.</p>"}
slides_data[99] = {"content": "<p>Экосистема — преимущества.</p>"}
slides_data[100] = {"content": "<p>Резервное копирование — общие возможности.</p>"}
slides_data[101] = {"content": "<p>Резервное копирование — стратегии.</p>"}
slides_data[102] = {"content": "<p>VDI — архитектура.</p>"}
slides_data[103] = {"content": "<p>VDI — сценарии использования.</p>"}
slides_data[104] = {"content": "<p>Мониторинг — панель управления.</p>"}
slides_data[105] = {"content": "<p>Мониторинг — алерты и графики.</p>"}
slides_data[106] = {"content": "<p>Миграция — подготовка и планирование.</p>"}

# ------------------------------------------------------------
# Группировка разделов (название плитки → список слайдов)
# ------------------------------------------------------------
sections = {
    "Экосистема Горизонт-ВС": [96, 98, 99],
    "Система серверной виртуализации": [98, 5, 7, 8, 10, 11, 12, 13, 14, 16, 18, 19, 20, 21, 23, 24, 26, 27, 28, 30, 32, 33, 34, 35, 36, 38, 39, 40, 41],
    "Система резервного копирования": [100, 101, 42, 44, 45, 46, 48, 49, 51, 52, 54],
    "Горизонт-ВС VDI": [102, 103, 55, 57, 58, 60, 61, 62, 64, 65, 67, 68],
    "Система мониторинга": [104, 105] + list(range(71, 84)),
    "Горизонт-ВС Миграция": [106] + list(range(84, 90)),
    "О продукте": [91, 92, 93, 94, 95],
    "Контакты": [90],
    "ФСТЭК и безопасная разработка": [96, 97]
}

# Порядок плиток в сетке 3×3 (9 штук)
ordered_tiles = [
    "Экосистема Горизонт-ВС",
    "Система серверной виртуализации",
    "Система резервного копирования",
    "Горизонт-ВС VDI",
    "Система мониторинга",
    "Горизонт-ВС Миграция",
    "О продукте",
    "Контакты",
    "ФСТЭК и безопасная разработка"
]

# ------------------------------------------------------------
# Функция проверки наличия изображения
# ------------------------------------------------------------
def image_exists(slide_num):
    for ext in ['.png', '.jpg', '.jpeg']:
        path = os.path.join('static', 'slides', f"{slide_num}{ext}")
        if os.path.exists(path):
            return f"/static/slides/{slide_num}{ext}"
    return None

# ------------------------------------------------------------
# Маршруты Flask
# ------------------------------------------------------------
@app.route('/')
def index():
    return render_template('index.html', tile_names=ordered_tiles, sections=sections)

@app.route('/api/slide/<int:slide_num>')
def get_slide(slide_num):
    img_url = image_exists(slide_num)
    if img_url:
        return jsonify({"has_image": True, "image_url": img_url, "content": None})
    else:
        data = slides_data.get(slide_num, {"content": "<p>Нет данных</p>"})
        return jsonify({
            "has_image": False,
            "image_url": None,
            "content": data.get("content", "<p>Нет описания</p>")
        })

if __name__ == '__main__':
    app.run(debug=True, host='0.0.0.0', port=5000)